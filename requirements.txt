# MantisClaw Dependencies

# Core
pyyaml>=6.0
python-dotenv>=1.0

# Dashboard (FastAPI + SSE)
fastapi>=0.100
uvicorn[standard]>=0.20
jinja2>=3.1
python-multipart>=0.0.5
aiosqlite>=0.19

# LLM Backends — Local-first, zero dependencies:
#   lmstudio: Kein Package nötig (OpenAI-kompatible HTTP API)
#   ollama:   Kein Package nötig (REST API)
#
# Cloud (optional, nur wenn gewünscht):
# openai>=1.0
# anthropic>=0.20
